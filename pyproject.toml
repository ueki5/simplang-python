[project]
name = "minclang-python"
version = "0.1.0"
description = "Minimal compiler for arithmetic expressions"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]
